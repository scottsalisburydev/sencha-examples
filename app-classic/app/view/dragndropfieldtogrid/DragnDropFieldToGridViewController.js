Ext.define('Demo.view.dragndropfieldtogrid.DragnDropFieldToGridViewController', {
    extend: 'Ext.app.ViewController',
    alias: 'controller.dndfieldtogrid-fieldtogrid',

    onCellDrop: function (fieldName) {
        // var store = this.grid.store,
        //     sorter = store.sorters.first();

        // if (sorter && sorter.property === fieldName) {
        //     store.sort();
        // }
    },
});
